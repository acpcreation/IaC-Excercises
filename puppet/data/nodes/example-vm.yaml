---
# Example per-node Hiera data
# Copy to nodes/your-vm-hostname.yaml and customize

# Override global settings for this specific node
nodejs_install::nodejs_version: '20.x'
nodejs_install::npm_version: 'latest'
